feat(card): implement 4-step card close wizard

Add accounts step (CARD filter), update step (open-ended recurrences
with toggle-to-edit), confirm step (summary with collapsible sections),
and review step (success). Wire SmartFormProviderCard defaults and
form submit flow.
