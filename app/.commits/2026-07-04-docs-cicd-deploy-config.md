docs(cicd): update design spec with deploy configuration and env vars

Add documentation for:
- Docker Compose deploy configuration (image: vs build: pattern)
- Environment variables (GitHub Actions vars, docker .env)
- Server deployment workflow (prerequisites, commands, first-time setup)
- NEXT_PUBLIC_API_URL build-time variable explanation
- Updated "What's Still Open" with automated deploy and runtime injection options
